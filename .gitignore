# ============================================================
# Build artifacts
# ============================================================
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Build / coverage / generated dirs
bin/
build/
build-*/
coverage/
out/

# ============================================================
# IDE / OS / misc
# ============================================================
.vscode/
.idea/

# ============================================================
# Logs and large outputs
# ============================================================
*.log

# ============================================================
# Environment / data
# ============================================================
# Local env files with real config (tokens, IPs) — NOT in git.
# Use the corresponding `.example` file as a template and copy to
# the local filename. Example: `cp deployments/.env.bundled-full.example
# deployments/.env.bundled-full && $EDITOR deployments/.env.bundled-full`.
deployments/.env
deployments/.env.bundled
deployments/.env.bundled-full
deployments/.env.bundled-with-agent
deployments/.env.cocoindex
deployments/.env.llama
deployments/data/
data/

# ============================================================
# Local task tracker (used by Cline internally)
# ============================================================
task_progress.json
task_progress.md

# ============================================================
# Python caches
# ============================================================
__pycache__/
*.pyc
*.pyo
*.pyd

# ============================================================
# CGo bridge + llama.cpp source (vendored in c/)
# These are not temporary — they're real project files.
# Uncomment the next line only if c/ is treated as disposable.
# /c/
# ============================================================

# ============================================================
# Local build artifacts / scratch
# ============================================================
CMakeCache.txt
CMakeFiles/
*.err
balancer-linux
cppworker-stub
.docker_info.txt
configure-cuda.bat
build-llama-cuda.ps1

# ============================================================
# Temporary / diagnostic test directories
# ============================================================
stub-test-models/
tests/diag/
tests/multiclient/

# ============================================================
# Test results / coverage dumps
# ============================================================
test_results/

# ============================================================
# CocoIndex cache
# ============================================================
.cocoindex_code/

# ============================================================
# Anti-clutter: temporary debug / one-off artifacts in repo ROOT
# (real loads tests / debug scripts live in scripts/ instead)
# ============================================================

# One-off debug logs / result dumps in root
/test_output.txt
/test_result.txt
/test_result2.txt
/test_result*.txt
/test_results_final.txt
/final_run.txt
/final_tests.txt
/tmp_response.txt
/tmp_restart.log
/tmp_cppworker.log
/tmp_build.log
/build_gpu.log

# Typo'd scratch files
/utputFormat

# Throwaway python one-offs in repo root (real e2e tests live in scripts/)
/test_*.py
/apply_chat_template_fix.py
/check_compile.py
/fix_main.py
/register_backend.py
/run_template_test.py
/update_config.py

# Throwaway powershell one-offs in repo root
/test_*.ps1
/diagnose.ps1
/restart_stack.ps1
/check_cluster.ps1
/check_disk.ps1
/check_wsl_disk.ps1

# Throwaway tmp_* scratch files in repo root
/tmp_*.py
/tmp_*.ps1
/tmp_*.json
/tmp_*.err
/tmp_*.log
/tmp_*.txt
/tmp_*

# Throwaway cluster/backend JSON snapshots
/cluster*.json
/add_*.json
models/

balancer_test_build

# Round 11 (2026-07-10): consolidated root cleanup — all one-shot dev
# scripts moved to scripts/, never committed at repo root.
apply_*.py
*_helper.py
inspect_out.txt
*.7z
*.zip
*.tar.gz

# Old task tracker / plan files (replaced by plans/README.md roadmap)
plan.md
task_progress.md
task_progress.json
DEPLOYMENT.md

# Round 17 (2026-07-10): misc dev artifacts.
# One-off dev scripts that should live in scripts/ instead of source tree.
cmd/cppworker/find.py
# Backup directories (created by IDE / manual cp -r before refactor).
cmd/cppworker/.backup-*/
cmd/agent/.backup-*/
internal/**/*.backup-*/

# Playwright / Node tooling (Sprint 1 Day 2 - 2026-06-29)
# Note: we COMMIT package-lock.json for reproducible installs and we
# COMMIT the -snapshots folder so visual baselines are reviewed in PRs.
/node_modules/
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/
/tests/visual/**/*-actual.png
/tests/visual/**/*-diff.png

# Round 35 (2026-08-13) cleanup: one-off archive of files moved out of
# repo root (old round backup dirs, stale logs, scratch scripts).
# User can `mv .cleanup_archive_20260812 /tmp/` to permanently delete.
.cleanup_archive_*/

